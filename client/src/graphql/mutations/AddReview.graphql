mutation AddReview(
  $title: String!
  $content: String
  $mediaId: ID
) {
  addReview(title: $title, content: $content, mediaId: $mediaId) {
    id
    score
    title
    content
    media {
      id
      title
    }
  }
}
